{
  "scripts": {
    "test": "jest",
    "prepare": "husky install"
  },
  "devDependencies": {
    "husky": "^7.0.4",
    "jest": "^27.3.1",
    "jest-junit": "^13.0.0",
    "lint-staged": "^12.1.2",
    "prettier": "^2.4.1"
  },
  "lint-staged": {
    "*": "./scripts/generate_docs.sh",
    "*.{js,css,md}": "prettier --write"
  }
}
